#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    string item;

    cin >> n >> n;
    cout << n << endl;

    while (n--) {
        cin >> item;
        cout << item << endl;
    }

    return 0;
}